apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-service
  namespace: todo-dev
  labels:
    app: todo-frontend
    tier: frontend
spec:
  selector:
    app: todo-frontend
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP